swarm:
   port:
      offset: 1
   context:
      path: /consumer
kafka:
   bootstrap:
      servers: localhost:9091,localhost:9092
   group:
      id: appName
   topics: my-topic,another-topic
   commitAsync: false
   poll:
      interval: 1000
